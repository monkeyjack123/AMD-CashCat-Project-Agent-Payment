# Showcase Code

This folder contains selected public-safe source excerpts from the private `agent-payment` codebase.

Purpose:

- show judges and reviewers how the runtime demo is implemented
- expose the first-wedge demo logic without publishing the full internal repository

Notes:

- these files are excerpts, not a standalone production app
- sensitive modules and commercialization details are intentionally omitted
- some imports point to modules that remain private
